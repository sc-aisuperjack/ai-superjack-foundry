# API Endpoints

## GET /health
Returns service health.

## POST /v1/documents/upload
Stores a document and creates chunks.

## POST /v1/chat/query
Runs retrieval and answer orchestration.

## GET /v1/traces/{trace_id}
Returns a stored orchestration trace.
